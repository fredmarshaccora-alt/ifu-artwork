# Server runtime dependencies for the IFU compute back-end (Render).
# Pinned to what's known-good locally.  `cadquery` pulls cadquery-ocp,
# which bundles prebuilt OpenCascade (OCCT) Linux wheels -- so the image
# does NOT need to compile OCCT.  Dev/visualisation extras (ocp_vscode,
# ocp-tessellate, casadi, etc.) are intentionally omitted.
Flask==3.1.3
flask-sock==0.7.0
cadquery==2.7.0
trimesh==4.12.0
numpy==2.4.2
scipy==1.17.1
shapely==2.1.2
svglib==1.6.0
reportlab==4.4.10
pillow==12.1.1
requests==2.32.5
requests-oauthlib==2.0.0
requests-toolbelt==1.0.0
python-dotenv==1.0.0
# Supabase magic-link auth: verify user JWTs against the project's public
# JWKS (ES256).  [crypto] pulls `cryptography` for asymmetric verification.
PyJWT[crypto]==2.9.0
# Production WSGI server (replaces Flask's dev server).
gunicorn==23.0.0
