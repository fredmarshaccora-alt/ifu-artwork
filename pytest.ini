[pytest]
testpaths = tests
markers =
    unit: fast (<1s), no server, no STEP files
    integration: needs STEP file + server, ~30s
    e2e: needs server + Playwright, full browser
addopts = -ra --strict-markers
